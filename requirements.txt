# Core dependencies
mcp>=0.9.0              # Model Context Protocol SDK
oletools>=0.60          # Extract VBA from Office files
openpyxl>=3.1.0         # Parse Excel OOXML files

# Optional dependencies
pywin32>=306; platform_system=="Windows"  # Windows COM APIs (optional)

# Development dependencies (comment out for production)
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
